diff --git a/CMakeLists.txt b/CMakeLists.txt
index 4073ac5..2f85bbc 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -19,7 +19,7 @@ set(CMAKE_CUDA_STANDARD_REQUIRED ON)
 # Set CUDA host compiler
 set(CMAKE_CUDA_HOST_COMPILER ${CMAKE_CXX_COMPILER})
 set(CMAKE_CUDA_SEPARABLE_COMPILATION ON)
-set(CMAKE_CUDA_ARCHITECTURES 60 61 62 70 75 80 86)
+set(CMAKE_CUDA_ARCHITECTURES 60 61 62 70 75 80 86 90)
 set(CUDA_PROPAGATE_HOST_FLAGS ON)
 set(CMAKE_EXPORT_COMPILE_COMMANDS ON)
 
diff --git a/examples/cuSZp.cpp b/examples/cuSZp.cpp
index baaad76..7a06555 100644
--- a/examples/cuSZp.cpp
+++ b/examples/cuSZp.cpp
@@ -239,9 +239,15 @@ int main(int argc, char* argv[])
     }
 
     // cuSZp compression.
+    // Timed as an average over TIMING_REPEATS back-to-back launches — see
+    // cuSZp_entry_*.cu for the real fix (scratch-buffer cudaMalloc/cudaFree caching)
+    // that this batching complements; kept for consistency with cuSZp-V2's driver.
+    const int TIMING_REPEATS = 100;
     timer_GPU.StartCounter(); // set timer
-    cuSZp_compress(d_oriData, d_cmpBytes, nbEle, &cmpSize, errorBound, processingDim, dims, dataType, encodingMode, stream);
-    float cmpTime = timer_GPU.GetCounter();
+    for (int i = 0; i < TIMING_REPEATS; i++) {
+        cuSZp_compress(d_oriData, d_cmpBytes, nbEle, &cmpSize, errorBound, processingDim, dims, dataType, encodingMode, stream);
+    }
+    float cmpTime = timer_GPU.GetCounter() / TIMING_REPEATS;
 
     // Transfer compressed data to CPU then back to GPU, making sure compression ratio is correct.
     // No need to add this part for real-world usages, this is only for testing compresion ratio correcness.
@@ -255,10 +261,12 @@ int main(int argc, char* argv[])
     }
     cudaMemcpy(d_cmpBytes, cmpBytes_dup, cmpSize*sizeof(unsigned char), cudaMemcpyHostToDevice);
 
-    // cuSZp decompression.
+    // cuSZp decompression. Batched the same way as compression, see above.
     timer_GPU.StartCounter(); // set timer
-    cuSZp_decompress(d_decData, d_cmpBytes, nbEle, cmpSize, errorBound, processingDim, dims, dataType, encodingMode, stream);
-    float decTime = timer_GPU.GetCounter();
+    for (int i = 0; i < TIMING_REPEATS; i++) {
+        cuSZp_decompress(d_decData, d_cmpBytes, nbEle, cmpSize, errorBound, processingDim, dims, dataType, encodingMode, stream);
+    }
+    float decTime = timer_GPU.GetCounter() / TIMING_REPEATS;
 
     // Print result.
     printf("cuSZp finished!\n");
diff --git a/src/cuSZp_entry_1D_f32.cu b/src/cuSZp_entry_1D_f32.cu
index 5c27af0..cf2a71f 100644
--- a/src/cuSZp_entry_1D_f32.cu
+++ b/src/cuSZp_entry_1D_f32.cu
@@ -1,6 +1,40 @@
 #include "cuSZp_entry_1D_f32.h"
 #include "cuSZp_kernels_1D_f32.h"
 
+// Scratch-buffer cache: cuSZp originally cudaMalloc'd + cudaFree'd 3 small scratch
+// arrays (d_cmpOffset/d_locOffset/d_flag) on every compress/decompress call. Cheap
+// on bare-metal GPUs, but on some platforms (observed on a GPU-passthrough cloud
+// VM) cudaMalloc/cudaFree carry large, highly variable latency (seen up to several
+// hundred ms for a single call) that dwarfs actual kernel time and dominates any
+// per-call timing. One cache per function below, grow-allocated once per distinct
+// cmpOffSize and reused across calls (still memset to 0 every call — correctness
+// unaffected). Freed only at process exit, fine for this CLI.
+namespace {
+struct CuszpScratch {
+    unsigned int* d_cmpOffset = nullptr;
+    unsigned int* d_locOffset = nullptr;
+    int* d_flag = nullptr;
+    size_t capacity = 0;
+
+    void ensure(size_t cmpOffSize) {
+        if (cmpOffSize <= capacity) return;
+        if (d_cmpOffset) cudaFree(d_cmpOffset);
+        if (d_locOffset) cudaFree(d_locOffset);
+        if (d_flag) cudaFree(d_flag);
+        cudaMalloc((void**)&d_cmpOffset, sizeof(unsigned int) * cmpOffSize);
+        cudaMalloc((void**)&d_locOffset, sizeof(unsigned int) * cmpOffSize);
+        cudaMalloc((void**)&d_flag, sizeof(int) * cmpOffSize);
+        capacity = cmpOffSize;
+    }
+};
+CuszpScratch g_scratch_cuSZp_compress_1D_fixed_f32;
+CuszpScratch g_scratch_cuSZp_decompress_1D_fixed_f32;
+CuszpScratch g_scratch_cuSZp_compress_1D_plain_f32;
+CuszpScratch g_scratch_cuSZp_decompress_1D_plain_f32;
+CuszpScratch g_scratch_cuSZp_compress_1D_outlier_f32;
+CuszpScratch g_scratch_cuSZp_decompress_1D_outlier_f32;
+}  // namespace
+
 /** ************************************************************************
  * @brief cuSZp end-to-end compression API for device pointers
  *        Compression is executed in GPU.
@@ -22,15 +56,13 @@ void cuSZp_compress_1D_fixed_f32(float* d_oriData, unsigned char* d_cmpBytes, si
     int cmpOffSize = gsize + 1;
 
     // Initializing global memory for GPU compression.
-    unsigned int* d_cmpOffset;
-    unsigned int* d_locOffset;
-    int* d_flag;
+    g_scratch_cuSZp_compress_1D_fixed_f32.ensure(cmpOffSize);
+    unsigned int* d_cmpOffset = g_scratch_cuSZp_compress_1D_fixed_f32.d_cmpOffset;
+    unsigned int* d_locOffset = g_scratch_cuSZp_compress_1D_fixed_f32.d_locOffset;
+    int* d_flag = g_scratch_cuSZp_compress_1D_fixed_f32.d_flag;
     unsigned int glob_sync;
-    cudaMalloc((void**)&d_cmpOffset, sizeof(unsigned int)*cmpOffSize);
     cudaMemset(d_cmpOffset, 0, sizeof(unsigned int)*cmpOffSize);
-    cudaMalloc((void**)&d_locOffset, sizeof(unsigned int)*cmpOffSize);
     cudaMemset(d_locOffset, 0, sizeof(unsigned int)*cmpOffSize);
-    cudaMalloc((void**)&d_flag, sizeof(int)*cmpOffSize);
     cudaMemset(d_flag, 0, sizeof(int)*cmpOffSize);
 
     // cuSZp GPU compression.
@@ -42,10 +74,6 @@ void cuSZp_compress_1D_fixed_f32(float* d_oriData, unsigned char* d_cmpBytes, si
     cudaMemcpy(&glob_sync, d_cmpOffset+cmpOffSize-1, sizeof(unsigned int), cudaMemcpyDeviceToHost);
     *cmpSize = (size_t)glob_sync + (nbEle+tblock_size*thread_chunk-1)/(tblock_size*thread_chunk)*(tblock_size*thread_chunk)/32;
 
-    // Free memory that is used.
-    cudaFree(d_cmpOffset);
-    cudaFree(d_locOffset);
-    cudaFree(d_flag);
 }
 
  /** ************************************************************************
@@ -70,14 +98,12 @@ void cuSZp_decompress_1D_fixed_f32(float* d_decData, unsigned char* d_cmpBytes,
     int cmpOffSize = gsize + 1;
 
     // Initializing global memory for GPU decompression.
-    unsigned int* d_cmpOffset;
-    unsigned int* d_locOffset;
-    int* d_flag;
-    cudaMalloc((void**)&d_cmpOffset, sizeof(unsigned int)*cmpOffSize);
+    g_scratch_cuSZp_decompress_1D_fixed_f32.ensure(cmpOffSize);
+    unsigned int* d_cmpOffset = g_scratch_cuSZp_decompress_1D_fixed_f32.d_cmpOffset;
+    unsigned int* d_locOffset = g_scratch_cuSZp_decompress_1D_fixed_f32.d_locOffset;
+    int* d_flag = g_scratch_cuSZp_decompress_1D_fixed_f32.d_flag;
     cudaMemset(d_cmpOffset, 0, sizeof(unsigned int)*cmpOffSize);
-    cudaMalloc((void**)&d_locOffset, sizeof(unsigned int)*cmpOffSize);
     cudaMemset(d_locOffset, 0, sizeof(unsigned int)*cmpOffSize);
-    cudaMalloc((void**)&d_flag, sizeof(int)*cmpOffSize);
     cudaMemset(d_flag, 0, sizeof(int)*cmpOffSize);
 
     // cuSZp GPU decompression.
@@ -85,10 +111,6 @@ void cuSZp_decompress_1D_fixed_f32(float* d_decData, unsigned char* d_cmpBytes,
     dim3 gridSize(gsize);
     cuSZp_decompress_kernel_1D_fixed_f32<<<gridSize, blockSize, sizeof(unsigned int)*2, stream>>>(d_decData, d_cmpBytes, d_cmpOffset, d_locOffset, d_flag, errorBound, nbEle);
     
-    // Free memory that is used.
-    cudaFree(d_cmpOffset);
-    cudaFree(d_locOffset);
-    cudaFree(d_flag);
 }
 
 /** ************************************************************************
@@ -112,15 +134,13 @@ void cuSZp_compress_1D_plain_f32(float* d_oriData, unsigned char* d_cmpBytes, si
     int cmpOffSize = gsize + 1;
 
     // Initializing global memory for GPU compression.
-    unsigned int* d_cmpOffset;
-    unsigned int* d_locOffset;
-    int* d_flag;
+    g_scratch_cuSZp_compress_1D_plain_f32.ensure(cmpOffSize);
+    unsigned int* d_cmpOffset = g_scratch_cuSZp_compress_1D_plain_f32.d_cmpOffset;
+    unsigned int* d_locOffset = g_scratch_cuSZp_compress_1D_plain_f32.d_locOffset;
+    int* d_flag = g_scratch_cuSZp_compress_1D_plain_f32.d_flag;
     unsigned int glob_sync;
-    cudaMalloc((void**)&d_cmpOffset, sizeof(unsigned int)*cmpOffSize);
     cudaMemset(d_cmpOffset, 0, sizeof(unsigned int)*cmpOffSize);
-    cudaMalloc((void**)&d_locOffset, sizeof(unsigned int)*cmpOffSize);
     cudaMemset(d_locOffset, 0, sizeof(unsigned int)*cmpOffSize);
-    cudaMalloc((void**)&d_flag, sizeof(int)*cmpOffSize);
     cudaMemset(d_flag, 0, sizeof(int)*cmpOffSize);
 
     // cuSZp GPU compression.
@@ -132,10 +152,6 @@ void cuSZp_compress_1D_plain_f32(float* d_oriData, unsigned char* d_cmpBytes, si
     cudaMemcpy(&glob_sync, d_cmpOffset+cmpOffSize-1, sizeof(unsigned int), cudaMemcpyDeviceToHost);
     *cmpSize = (size_t)glob_sync + (nbEle+tblock_size*thread_chunk-1)/(tblock_size*thread_chunk)*(tblock_size*thread_chunk)/32;
 
-    // Free memory that is used.
-    cudaFree(d_cmpOffset);
-    cudaFree(d_locOffset);
-    cudaFree(d_flag);
 }
 
  /** ************************************************************************
@@ -160,14 +176,12 @@ void cuSZp_decompress_1D_plain_f32(float* d_decData, unsigned char* d_cmpBytes,
     int cmpOffSize = gsize + 1;
 
     // Initializing global memory for GPU decompression.
-    unsigned int* d_cmpOffset;
-    unsigned int* d_locOffset;
-    int* d_flag;
-    cudaMalloc((void**)&d_cmpOffset, sizeof(unsigned int)*cmpOffSize);
+    g_scratch_cuSZp_decompress_1D_plain_f32.ensure(cmpOffSize);
+    unsigned int* d_cmpOffset = g_scratch_cuSZp_decompress_1D_plain_f32.d_cmpOffset;
+    unsigned int* d_locOffset = g_scratch_cuSZp_decompress_1D_plain_f32.d_locOffset;
+    int* d_flag = g_scratch_cuSZp_decompress_1D_plain_f32.d_flag;
     cudaMemset(d_cmpOffset, 0, sizeof(unsigned int)*cmpOffSize);
-    cudaMalloc((void**)&d_locOffset, sizeof(unsigned int)*cmpOffSize);
     cudaMemset(d_locOffset, 0, sizeof(unsigned int)*cmpOffSize);
-    cudaMalloc((void**)&d_flag, sizeof(int)*cmpOffSize);
     cudaMemset(d_flag, 0, sizeof(int)*cmpOffSize);
 
     // cuSZp GPU decompression.
@@ -175,10 +189,6 @@ void cuSZp_decompress_1D_plain_f32(float* d_decData, unsigned char* d_cmpBytes,
     dim3 gridSize(gsize);
     cuSZp_decompress_kernel_1D_plain_f32<<<gridSize, blockSize, sizeof(unsigned int)*2, stream>>>(d_decData, d_cmpBytes, d_cmpOffset, d_locOffset, d_flag, errorBound, nbEle);
     
-    // Free memory that is used.
-    cudaFree(d_cmpOffset);
-    cudaFree(d_locOffset);
-    cudaFree(d_flag);
 }
 
 /** ************************************************************************
@@ -202,15 +212,13 @@ void cuSZp_compress_1D_outlier_f32(float* d_oriData, unsigned char* d_cmpBytes,
     int cmpOffSize = gsize + 1;
 
     // Initializing global memory for GPU compression.
-    unsigned int* d_cmpOffset;
-    unsigned int* d_locOffset;
-    int* d_flag;
+    g_scratch_cuSZp_compress_1D_outlier_f32.ensure(cmpOffSize);
+    unsigned int* d_cmpOffset = g_scratch_cuSZp_compress_1D_outlier_f32.d_cmpOffset;
+    unsigned int* d_locOffset = g_scratch_cuSZp_compress_1D_outlier_f32.d_locOffset;
+    int* d_flag = g_scratch_cuSZp_compress_1D_outlier_f32.d_flag;
     unsigned int glob_sync;
-    cudaMalloc((void**)&d_cmpOffset, sizeof(unsigned int)*cmpOffSize);
     cudaMemset(d_cmpOffset, 0, sizeof(unsigned int)*cmpOffSize);
-    cudaMalloc((void**)&d_locOffset, sizeof(unsigned int)*cmpOffSize);
     cudaMemset(d_locOffset, 0, sizeof(unsigned int)*cmpOffSize);
-    cudaMalloc((void**)&d_flag, sizeof(int)*cmpOffSize);
     cudaMemset(d_flag, 0, sizeof(int)*cmpOffSize);
 
     // cuSZp GPU compression.
@@ -222,10 +230,6 @@ void cuSZp_compress_1D_outlier_f32(float* d_oriData, unsigned char* d_cmpBytes,
     cudaMemcpy(&glob_sync, d_cmpOffset+cmpOffSize-1, sizeof(unsigned int), cudaMemcpyDeviceToHost);
     *cmpSize = (size_t)glob_sync + (nbEle+tblock_size*thread_chunk-1)/(tblock_size*thread_chunk)*(tblock_size*thread_chunk)/32;
 
-    // Free memory that is used.
-    cudaFree(d_cmpOffset);
-    cudaFree(d_locOffset);
-    cudaFree(d_flag);
 }
 
  /** ************************************************************************
@@ -250,14 +254,12 @@ void cuSZp_decompress_1D_outlier_f32(float* d_decData, unsigned char* d_cmpBytes
     int cmpOffSize = gsize + 1;
 
     // Initializing global memory for GPU decompression.
-    unsigned int* d_cmpOffset;
-    unsigned int* d_locOffset;
-    int* d_flag;
-    cudaMalloc((void**)&d_cmpOffset, sizeof(unsigned int)*cmpOffSize);
+    g_scratch_cuSZp_decompress_1D_outlier_f32.ensure(cmpOffSize);
+    unsigned int* d_cmpOffset = g_scratch_cuSZp_decompress_1D_outlier_f32.d_cmpOffset;
+    unsigned int* d_locOffset = g_scratch_cuSZp_decompress_1D_outlier_f32.d_locOffset;
+    int* d_flag = g_scratch_cuSZp_decompress_1D_outlier_f32.d_flag;
     cudaMemset(d_cmpOffset, 0, sizeof(unsigned int)*cmpOffSize);
-    cudaMalloc((void**)&d_locOffset, sizeof(unsigned int)*cmpOffSize);
     cudaMemset(d_locOffset, 0, sizeof(unsigned int)*cmpOffSize);
-    cudaMalloc((void**)&d_flag, sizeof(int)*cmpOffSize);
     cudaMemset(d_flag, 0, sizeof(int)*cmpOffSize);
 
     // cuSZp GPU decompression.
@@ -265,8 +267,4 @@ void cuSZp_decompress_1D_outlier_f32(float* d_decData, unsigned char* d_cmpBytes
     dim3 gridSize(gsize);
     cuSZp_decompress_kernel_1D_outlier_f32<<<gridSize, blockSize, sizeof(unsigned int)*2, stream>>>(d_decData, d_cmpBytes, d_cmpOffset, d_locOffset, d_flag, errorBound, nbEle);
     
-    // Free memory that is used.
-    cudaFree(d_cmpOffset);
-    cudaFree(d_locOffset);
-    cudaFree(d_flag);
 }
\ No newline at end of file
diff --git a/src/cuSZp_entry_1D_f64.cu b/src/cuSZp_entry_1D_f64.cu
index 87cb7c4..bdbfa3f 100644
--- a/src/cuSZp_entry_1D_f64.cu
+++ b/src/cuSZp_entry_1D_f64.cu
@@ -1,6 +1,40 @@
 #include "cuSZp_entry_1D_f64.h"
 #include "cuSZp_kernels_1D_f64.h"
 
+// Scratch-buffer cache: cuSZp originally cudaMalloc'd + cudaFree'd 3 small scratch
+// arrays (d_cmpOffset/d_locOffset/d_flag) on every compress/decompress call. Cheap
+// on bare-metal GPUs, but on some platforms (observed on a GPU-passthrough cloud
+// VM) cudaMalloc/cudaFree carry large, highly variable latency (seen up to several
+// hundred ms for a single call) that dwarfs actual kernel time and dominates any
+// per-call timing. One cache per function below, grow-allocated once per distinct
+// cmpOffSize and reused across calls (still memset to 0 every call — correctness
+// unaffected). Freed only at process exit, fine for this CLI.
+namespace {
+struct CuszpScratch {
+    unsigned int* d_cmpOffset = nullptr;
+    unsigned int* d_locOffset = nullptr;
+    int* d_flag = nullptr;
+    size_t capacity = 0;
+
+    void ensure(size_t cmpOffSize) {
+        if (cmpOffSize <= capacity) return;
+        if (d_cmpOffset) cudaFree(d_cmpOffset);
+        if (d_locOffset) cudaFree(d_locOffset);
+        if (d_flag) cudaFree(d_flag);
+        cudaMalloc((void**)&d_cmpOffset, sizeof(unsigned int) * cmpOffSize);
+        cudaMalloc((void**)&d_locOffset, sizeof(unsigned int) * cmpOffSize);
+        cudaMalloc((void**)&d_flag, sizeof(int) * cmpOffSize);
+        capacity = cmpOffSize;
+    }
+};
+CuszpScratch g_scratch_cuSZp_compress_1D_fixed_f64;
+CuszpScratch g_scratch_cuSZp_decompress_1D_fixed_f64;
+CuszpScratch g_scratch_cuSZp_compress_1D_plain_f64;
+CuszpScratch g_scratch_cuSZp_decompress_1D_plain_f64;
+CuszpScratch g_scratch_cuSZp_compress_1D_outlier_f64;
+CuszpScratch g_scratch_cuSZp_decompress_1D_outlier_f64;
+}  // namespace
+
 /** ************************************************************************
  * @brief cuSZp end-to-end compression API for device pointers
  *        Compression is executed in GPU.
@@ -22,15 +56,13 @@ void cuSZp_compress_1D_fixed_f64(double* d_oriData, unsigned char* d_cmpBytes, s
     int cmpOffSize = gsize + 1;
 
     // Initializing global memory for GPU compression.
-    unsigned int* d_cmpOffset;
-    unsigned int* d_locOffset;
-    int* d_flag;
+    g_scratch_cuSZp_compress_1D_fixed_f64.ensure(cmpOffSize);
+    unsigned int* d_cmpOffset = g_scratch_cuSZp_compress_1D_fixed_f64.d_cmpOffset;
+    unsigned int* d_locOffset = g_scratch_cuSZp_compress_1D_fixed_f64.d_locOffset;
+    int* d_flag = g_scratch_cuSZp_compress_1D_fixed_f64.d_flag;
     unsigned int glob_sync;
-    cudaMalloc((void**)&d_cmpOffset, sizeof(unsigned int)*cmpOffSize);
     cudaMemset(d_cmpOffset, 0, sizeof(unsigned int)*cmpOffSize);
-    cudaMalloc((void**)&d_locOffset, sizeof(unsigned int)*cmpOffSize);
     cudaMemset(d_locOffset, 0, sizeof(unsigned int)*cmpOffSize);
-    cudaMalloc((void**)&d_flag, sizeof(int)*cmpOffSize);
     cudaMemset(d_flag, 0, sizeof(int)*cmpOffSize);
 
     // cuSZp GPU compression.
@@ -42,10 +74,6 @@ void cuSZp_compress_1D_fixed_f64(double* d_oriData, unsigned char* d_cmpBytes, s
     cudaMemcpy(&glob_sync, d_cmpOffset+cmpOffSize-1, sizeof(unsigned int), cudaMemcpyDeviceToHost);
     *cmpSize = (size_t)glob_sync + (nbEle+tblock_size*thread_chunk-1)/(tblock_size*thread_chunk)*(tblock_size*thread_chunk)/32;
 
-    // Free memory that is used.
-    cudaFree(d_cmpOffset);
-    cudaFree(d_locOffset);
-    cudaFree(d_flag);
 }
 
  /** ************************************************************************
@@ -70,14 +98,12 @@ void cuSZp_decompress_1D_fixed_f64(double* d_decData, unsigned char* d_cmpBytes,
     int cmpOffSize = gsize + 1;
 
     // Initializing global memory for GPU decompression.
-    unsigned int* d_cmpOffset;
-    unsigned int* d_locOffset;
-    int* d_flag;
-    cudaMalloc((void**)&d_cmpOffset, sizeof(unsigned int)*cmpOffSize);
+    g_scratch_cuSZp_decompress_1D_fixed_f64.ensure(cmpOffSize);
+    unsigned int* d_cmpOffset = g_scratch_cuSZp_decompress_1D_fixed_f64.d_cmpOffset;
+    unsigned int* d_locOffset = g_scratch_cuSZp_decompress_1D_fixed_f64.d_locOffset;
+    int* d_flag = g_scratch_cuSZp_decompress_1D_fixed_f64.d_flag;
     cudaMemset(d_cmpOffset, 0, sizeof(unsigned int)*cmpOffSize);
-    cudaMalloc((void**)&d_locOffset, sizeof(unsigned int)*cmpOffSize);
     cudaMemset(d_locOffset, 0, sizeof(unsigned int)*cmpOffSize);
-    cudaMalloc((void**)&d_flag, sizeof(int)*cmpOffSize);
     cudaMemset(d_flag, 0, sizeof(int)*cmpOffSize);
 
     // cuSZp GPU decompression.
@@ -85,10 +111,6 @@ void cuSZp_decompress_1D_fixed_f64(double* d_decData, unsigned char* d_cmpBytes,
     dim3 gridSize(gsize);
     cuSZp_decompress_kernel_1D_fixed_f64<<<gridSize, blockSize, sizeof(unsigned int)*2, stream>>>(d_decData, d_cmpBytes, d_cmpOffset, d_locOffset, d_flag, errorBound, nbEle);
     
-    // Free memory that is used.
-    cudaFree(d_cmpOffset);
-    cudaFree(d_locOffset);
-    cudaFree(d_flag);
 }
 
 /** ************************************************************************
@@ -112,15 +134,13 @@ void cuSZp_compress_1D_plain_f64(double* d_oriData, unsigned char* d_cmpBytes, s
     int cmpOffSize = gsize + 1;
 
     // Initializing global memory for GPU compression.
-    unsigned int* d_cmpOffset;
-    unsigned int* d_locOffset;
-    int* d_flag;
+    g_scratch_cuSZp_compress_1D_plain_f64.ensure(cmpOffSize);
+    unsigned int* d_cmpOffset = g_scratch_cuSZp_compress_1D_plain_f64.d_cmpOffset;
+    unsigned int* d_locOffset = g_scratch_cuSZp_compress_1D_plain_f64.d_locOffset;
+    int* d_flag = g_scratch_cuSZp_compress_1D_plain_f64.d_flag;
     unsigned int glob_sync;
-    cudaMalloc((void**)&d_cmpOffset, sizeof(unsigned int)*cmpOffSize);
     cudaMemset(d_cmpOffset, 0, sizeof(unsigned int)*cmpOffSize);
-    cudaMalloc((void**)&d_locOffset, sizeof(unsigned int)*cmpOffSize);
     cudaMemset(d_locOffset, 0, sizeof(unsigned int)*cmpOffSize);
-    cudaMalloc((void**)&d_flag, sizeof(int)*cmpOffSize);
     cudaMemset(d_flag, 0, sizeof(int)*cmpOffSize);
 
     // cuSZp GPU compression.
@@ -132,10 +152,6 @@ void cuSZp_compress_1D_plain_f64(double* d_oriData, unsigned char* d_cmpBytes, s
     cudaMemcpy(&glob_sync, d_cmpOffset+cmpOffSize-1, sizeof(unsigned int), cudaMemcpyDeviceToHost);
     *cmpSize = (size_t)glob_sync + (nbEle+tblock_size*thread_chunk-1)/(tblock_size*thread_chunk)*(tblock_size*thread_chunk)/32;
 
-    // Free memory that is used.
-    cudaFree(d_cmpOffset);
-    cudaFree(d_locOffset);
-    cudaFree(d_flag);
 }
 
  /** ************************************************************************
@@ -160,14 +176,12 @@ void cuSZp_decompress_1D_plain_f64(double* d_decData, unsigned char* d_cmpBytes,
     int cmpOffSize = gsize + 1;
 
     // Initializing global memory for GPU decompression.
-    unsigned int* d_cmpOffset;
-    unsigned int* d_locOffset;
-    int* d_flag;
-    cudaMalloc((void**)&d_cmpOffset, sizeof(unsigned int)*cmpOffSize);
+    g_scratch_cuSZp_decompress_1D_plain_f64.ensure(cmpOffSize);
+    unsigned int* d_cmpOffset = g_scratch_cuSZp_decompress_1D_plain_f64.d_cmpOffset;
+    unsigned int* d_locOffset = g_scratch_cuSZp_decompress_1D_plain_f64.d_locOffset;
+    int* d_flag = g_scratch_cuSZp_decompress_1D_plain_f64.d_flag;
     cudaMemset(d_cmpOffset, 0, sizeof(unsigned int)*cmpOffSize);
-    cudaMalloc((void**)&d_locOffset, sizeof(unsigned int)*cmpOffSize);
     cudaMemset(d_locOffset, 0, sizeof(unsigned int)*cmpOffSize);
-    cudaMalloc((void**)&d_flag, sizeof(int)*cmpOffSize);
     cudaMemset(d_flag, 0, sizeof(int)*cmpOffSize);
 
     // cuSZp GPU decompression.
@@ -175,10 +189,6 @@ void cuSZp_decompress_1D_plain_f64(double* d_decData, unsigned char* d_cmpBytes,
     dim3 gridSize(gsize);
     cuSZp_decompress_kernel_1D_plain_f64<<<gridSize, blockSize, sizeof(unsigned int)*2, stream>>>(d_decData, d_cmpBytes, d_cmpOffset, d_locOffset, d_flag, errorBound, nbEle);
     
-    // Free memory that is used.
-    cudaFree(d_cmpOffset);
-    cudaFree(d_locOffset);
-    cudaFree(d_flag);
 }
 
 /** ************************************************************************
@@ -202,15 +212,13 @@ void cuSZp_compress_1D_outlier_f64(double* d_oriData, unsigned char* d_cmpBytes,
     int cmpOffSize = gsize + 1;
 
     // Initializing global memory for GPU compression.
-    unsigned int* d_cmpOffset;
-    unsigned int* d_locOffset;
-    int* d_flag;
+    g_scratch_cuSZp_compress_1D_outlier_f64.ensure(cmpOffSize);
+    unsigned int* d_cmpOffset = g_scratch_cuSZp_compress_1D_outlier_f64.d_cmpOffset;
+    unsigned int* d_locOffset = g_scratch_cuSZp_compress_1D_outlier_f64.d_locOffset;
+    int* d_flag = g_scratch_cuSZp_compress_1D_outlier_f64.d_flag;
     unsigned int glob_sync;
-    cudaMalloc((void**)&d_cmpOffset, sizeof(unsigned int)*cmpOffSize);
     cudaMemset(d_cmpOffset, 0, sizeof(unsigned int)*cmpOffSize);
-    cudaMalloc((void**)&d_locOffset, sizeof(unsigned int)*cmpOffSize);
     cudaMemset(d_locOffset, 0, sizeof(unsigned int)*cmpOffSize);
-    cudaMalloc((void**)&d_flag, sizeof(int)*cmpOffSize);
     cudaMemset(d_flag, 0, sizeof(int)*cmpOffSize);
 
     // cuSZp GPU compression.
@@ -222,10 +230,6 @@ void cuSZp_compress_1D_outlier_f64(double* d_oriData, unsigned char* d_cmpBytes,
     cudaMemcpy(&glob_sync, d_cmpOffset+cmpOffSize-1, sizeof(unsigned int), cudaMemcpyDeviceToHost);
     *cmpSize = (size_t)glob_sync + (nbEle+tblock_size*thread_chunk-1)/(tblock_size*thread_chunk)*(tblock_size*thread_chunk)/32;
 
-    // Free memory that is used.
-    cudaFree(d_cmpOffset);
-    cudaFree(d_locOffset);
-    cudaFree(d_flag);
 }
 
  /** ************************************************************************
@@ -250,14 +254,12 @@ void cuSZp_decompress_1D_outlier_f64(double* d_decData, unsigned char* d_cmpByte
     int cmpOffSize = gsize + 1;
 
     // Initializing global memory for GPU decompression.
-    unsigned int* d_cmpOffset;
-    unsigned int* d_locOffset;
-    int* d_flag;
-    cudaMalloc((void**)&d_cmpOffset, sizeof(unsigned int)*cmpOffSize);
+    g_scratch_cuSZp_decompress_1D_outlier_f64.ensure(cmpOffSize);
+    unsigned int* d_cmpOffset = g_scratch_cuSZp_decompress_1D_outlier_f64.d_cmpOffset;
+    unsigned int* d_locOffset = g_scratch_cuSZp_decompress_1D_outlier_f64.d_locOffset;
+    int* d_flag = g_scratch_cuSZp_decompress_1D_outlier_f64.d_flag;
     cudaMemset(d_cmpOffset, 0, sizeof(unsigned int)*cmpOffSize);
-    cudaMalloc((void**)&d_locOffset, sizeof(unsigned int)*cmpOffSize);
     cudaMemset(d_locOffset, 0, sizeof(unsigned int)*cmpOffSize);
-    cudaMalloc((void**)&d_flag, sizeof(int)*cmpOffSize);
     cudaMemset(d_flag, 0, sizeof(int)*cmpOffSize);
 
     // cuSZp GPU decompression.
@@ -265,8 +267,4 @@ void cuSZp_decompress_1D_outlier_f64(double* d_decData, unsigned char* d_cmpByte
     dim3 gridSize(gsize);
     cuSZp_decompress_kernel_1D_outlier_f64<<<gridSize, blockSize, sizeof(unsigned int)*2, stream>>>(d_decData, d_cmpBytes, d_cmpOffset, d_locOffset, d_flag, errorBound, nbEle);
     
-    // Free memory that is used.
-    cudaFree(d_cmpOffset);
-    cudaFree(d_locOffset);
-    cudaFree(d_flag);
 }
\ No newline at end of file
diff --git a/src/cuSZp_entry_2D_f32.cu b/src/cuSZp_entry_2D_f32.cu
index 025dd2b..2ae00e7 100644
--- a/src/cuSZp_entry_2D_f32.cu
+++ b/src/cuSZp_entry_2D_f32.cu
@@ -1,6 +1,40 @@
 #include "cuSZp_entry_2D_f32.h"
 #include "cuSZp_kernels_2D_f32.h"
 
+// Scratch-buffer cache: cuSZp originally cudaMalloc'd + cudaFree'd 3 small scratch
+// arrays (d_cmpOffset/d_locOffset/d_flag) on every compress/decompress call. Cheap
+// on bare-metal GPUs, but on some platforms (observed on a GPU-passthrough cloud
+// VM) cudaMalloc/cudaFree carry large, highly variable latency (seen up to several
+// hundred ms for a single call) that dwarfs actual kernel time and dominates any
+// per-call timing. One cache per function below, grow-allocated once per distinct
+// cmpOffSize and reused across calls (still memset to 0 every call — correctness
+// unaffected). Freed only at process exit, fine for this CLI.
+namespace {
+struct CuszpScratch {
+    unsigned int* d_cmpOffset = nullptr;
+    unsigned int* d_locOffset = nullptr;
+    int* d_flag = nullptr;
+    size_t capacity = 0;
+
+    void ensure(size_t cmpOffSize) {
+        if (cmpOffSize <= capacity) return;
+        if (d_cmpOffset) cudaFree(d_cmpOffset);
+        if (d_locOffset) cudaFree(d_locOffset);
+        if (d_flag) cudaFree(d_flag);
+        cudaMalloc((void**)&d_cmpOffset, sizeof(unsigned int) * cmpOffSize);
+        cudaMalloc((void**)&d_locOffset, sizeof(unsigned int) * cmpOffSize);
+        cudaMalloc((void**)&d_flag, sizeof(int) * cmpOffSize);
+        capacity = cmpOffSize;
+    }
+};
+CuszpScratch g_scratch_cuSZp_compress_2D_fixed_f32;
+CuszpScratch g_scratch_cuSZp_decompress_2D_fixed_f32;
+CuszpScratch g_scratch_cuSZp_compress_2D_plain_f32;
+CuszpScratch g_scratch_cuSZp_decompress_2D_plain_f32;
+CuszpScratch g_scratch_cuSZp_compress_2D_outlier_f32;
+CuszpScratch g_scratch_cuSZp_decompress_2D_outlier_f32;
+}  // namespace
+
 /** ************************************************************************
  * @brief cuSZp end-to-end compression API for device pointers
  *        Compression is executed in GPU.
@@ -26,15 +60,13 @@ void cuSZp_compress_2D_fixed_f32(float* d_oriData, unsigned char* d_cmpBytes, si
     int cmpOffSize = gsize + 1;
 
     // Initializing global memory for GPU compression.
-    unsigned int* d_cmpOffset;
-    unsigned int* d_locOffset;
-    int* d_flag;
+    g_scratch_cuSZp_compress_2D_fixed_f32.ensure(cmpOffSize);
+    unsigned int* d_cmpOffset = g_scratch_cuSZp_compress_2D_fixed_f32.d_cmpOffset;
+    unsigned int* d_locOffset = g_scratch_cuSZp_compress_2D_fixed_f32.d_locOffset;
+    int* d_flag = g_scratch_cuSZp_compress_2D_fixed_f32.d_flag;
     unsigned int glob_sync;
-    cudaMalloc((void**)&d_cmpOffset, sizeof(unsigned int)*cmpOffSize);
     cudaMemset(d_cmpOffset, 0, sizeof(unsigned int)*cmpOffSize);
-    cudaMalloc((void**)&d_locOffset, sizeof(unsigned int)*cmpOffSize);
     cudaMemset(d_locOffset, 0, sizeof(unsigned int)*cmpOffSize);
-    cudaMalloc((void**)&d_flag, sizeof(int)*cmpOffSize);
     cudaMemset(d_flag, 0, sizeof(int)*cmpOffSize);
 
     // Compression (implement for now, will update later)
@@ -46,10 +78,6 @@ void cuSZp_compress_2D_fixed_f32(float* d_oriData, unsigned char* d_cmpBytes, si
     cudaMemcpy(&glob_sync, d_cmpOffset+cmpOffSize-1, sizeof(unsigned int), cudaMemcpyDeviceToHost);
     *cmpSize = (size_t)glob_sync + blockNum;
 
-    // Free memory that is used.
-    cudaFree(d_cmpOffset);
-    cudaFree(d_locOffset);
-    cudaFree(d_flag);
 } 
 
 /** ************************************************************************
@@ -78,14 +106,12 @@ void cuSZp_decompress_2D_fixed_f32(float* d_decData, unsigned char* d_cmpBytes,
     int cmpOffSize = gsize + 1;
 
     // Initializing global memory for GPU compression.
-    unsigned int* d_cmpOffset;
-    unsigned int* d_locOffset;
-    int* d_flag;
-    cudaMalloc((void**)&d_cmpOffset, sizeof(unsigned int)*cmpOffSize);
+    g_scratch_cuSZp_decompress_2D_fixed_f32.ensure(cmpOffSize);
+    unsigned int* d_cmpOffset = g_scratch_cuSZp_decompress_2D_fixed_f32.d_cmpOffset;
+    unsigned int* d_locOffset = g_scratch_cuSZp_decompress_2D_fixed_f32.d_locOffset;
+    int* d_flag = g_scratch_cuSZp_decompress_2D_fixed_f32.d_flag;
     cudaMemset(d_cmpOffset, 0, sizeof(unsigned int)*cmpOffSize);
-    cudaMalloc((void**)&d_locOffset, sizeof(unsigned int)*cmpOffSize);
     cudaMemset(d_locOffset, 0, sizeof(unsigned int)*cmpOffSize);
-    cudaMalloc((void**)&d_flag, sizeof(int)*cmpOffSize);
     cudaMemset(d_flag, 0, sizeof(int)*cmpOffSize);
 
     // Decompression (implement for now, will update later with adaptive vectorization)
@@ -93,10 +119,6 @@ void cuSZp_decompress_2D_fixed_f32(float* d_decData, unsigned char* d_cmpBytes,
     dim3 gridSize(gsize);
     cuSZp_decompress_kernel_2D_fixed_vec4_f32<<<gridSize, blockSize, sizeof(unsigned int)*2, stream>>>(d_decData, d_cmpBytes, d_cmpOffset, d_locOffset, d_flag, blockNum, dims, errorBound);
 
-    // Free memory that is used.
-    cudaFree(d_cmpOffset);
-    cudaFree(d_locOffset);
-    cudaFree(d_flag);
 }
 
 /** ************************************************************************
@@ -124,15 +146,13 @@ void cuSZp_compress_2D_plain_f32(float* d_oriData, unsigned char* d_cmpBytes, si
     int cmpOffSize = gsize + 1;
 
     // Initializing global memory for GPU compression.
-    unsigned int* d_cmpOffset;
-    unsigned int* d_locOffset;
-    int* d_flag;
+    g_scratch_cuSZp_compress_2D_plain_f32.ensure(cmpOffSize);
+    unsigned int* d_cmpOffset = g_scratch_cuSZp_compress_2D_plain_f32.d_cmpOffset;
+    unsigned int* d_locOffset = g_scratch_cuSZp_compress_2D_plain_f32.d_locOffset;
+    int* d_flag = g_scratch_cuSZp_compress_2D_plain_f32.d_flag;
     unsigned int glob_sync;
-    cudaMalloc((void**)&d_cmpOffset, sizeof(unsigned int)*cmpOffSize);
     cudaMemset(d_cmpOffset, 0, sizeof(unsigned int)*cmpOffSize);
-    cudaMalloc((void**)&d_locOffset, sizeof(unsigned int)*cmpOffSize);
     cudaMemset(d_locOffset, 0, sizeof(unsigned int)*cmpOffSize);
-    cudaMalloc((void**)&d_flag, sizeof(int)*cmpOffSize);
     cudaMemset(d_flag, 0, sizeof(int)*cmpOffSize);
 
     // Compression (implement for now, will update later)
@@ -144,10 +164,6 @@ void cuSZp_compress_2D_plain_f32(float* d_oriData, unsigned char* d_cmpBytes, si
     cudaMemcpy(&glob_sync, d_cmpOffset+cmpOffSize-1, sizeof(unsigned int), cudaMemcpyDeviceToHost);
     *cmpSize = (size_t)glob_sync + blockNum;
 
-    // Free memory that is used.
-    cudaFree(d_cmpOffset);
-    cudaFree(d_locOffset);
-    cudaFree(d_flag);
 } 
 
 /** ************************************************************************
@@ -176,14 +192,12 @@ void cuSZp_decompress_2D_plain_f32(float* d_decData, unsigned char* d_cmpBytes,
     int cmpOffSize = gsize + 1;
 
     // Initializing global memory for GPU compression.
-    unsigned int* d_cmpOffset;
-    unsigned int* d_locOffset;
-    int* d_flag;
-    cudaMalloc((void**)&d_cmpOffset, sizeof(unsigned int)*cmpOffSize);
+    g_scratch_cuSZp_decompress_2D_plain_f32.ensure(cmpOffSize);
+    unsigned int* d_cmpOffset = g_scratch_cuSZp_decompress_2D_plain_f32.d_cmpOffset;
+    unsigned int* d_locOffset = g_scratch_cuSZp_decompress_2D_plain_f32.d_locOffset;
+    int* d_flag = g_scratch_cuSZp_decompress_2D_plain_f32.d_flag;
     cudaMemset(d_cmpOffset, 0, sizeof(unsigned int)*cmpOffSize);
-    cudaMalloc((void**)&d_locOffset, sizeof(unsigned int)*cmpOffSize);
     cudaMemset(d_locOffset, 0, sizeof(unsigned int)*cmpOffSize);
-    cudaMalloc((void**)&d_flag, sizeof(int)*cmpOffSize);
     cudaMemset(d_flag, 0, sizeof(int)*cmpOffSize);
 
     // Decompression (implement for now, will update later with adaptive vectorization)
@@ -191,10 +205,6 @@ void cuSZp_decompress_2D_plain_f32(float* d_decData, unsigned char* d_cmpBytes,
     dim3 gridSize(gsize);
     cuSZp_decompress_kernel_2D_plain_vec4_f32<<<gridSize, blockSize, sizeof(unsigned int)*2, stream>>>(d_decData, d_cmpBytes, d_cmpOffset, d_locOffset, d_flag, blockNum, dims, errorBound);
 
-    // Free memory that is used.
-    cudaFree(d_cmpOffset);
-    cudaFree(d_locOffset);
-    cudaFree(d_flag);
 }
 
 /** ************************************************************************
@@ -222,15 +232,13 @@ void cuSZp_compress_2D_outlier_f32(float* d_oriData, unsigned char* d_cmpBytes,
     int cmpOffSize = gsize + 1;
 
     // Initializing global memory for GPU compression.
-    unsigned int* d_cmpOffset;
-    unsigned int* d_locOffset;
-    int* d_flag;
+    g_scratch_cuSZp_compress_2D_outlier_f32.ensure(cmpOffSize);
+    unsigned int* d_cmpOffset = g_scratch_cuSZp_compress_2D_outlier_f32.d_cmpOffset;
+    unsigned int* d_locOffset = g_scratch_cuSZp_compress_2D_outlier_f32.d_locOffset;
+    int* d_flag = g_scratch_cuSZp_compress_2D_outlier_f32.d_flag;
     unsigned int glob_sync;
-    cudaMalloc((void**)&d_cmpOffset, sizeof(unsigned int)*cmpOffSize);
     cudaMemset(d_cmpOffset, 0, sizeof(unsigned int)*cmpOffSize);
-    cudaMalloc((void**)&d_locOffset, sizeof(unsigned int)*cmpOffSize);
     cudaMemset(d_locOffset, 0, sizeof(unsigned int)*cmpOffSize);
-    cudaMalloc((void**)&d_flag, sizeof(int)*cmpOffSize);
     cudaMemset(d_flag, 0, sizeof(int)*cmpOffSize);
 
     // Compression (implement for now, will update later)
@@ -242,10 +250,6 @@ void cuSZp_compress_2D_outlier_f32(float* d_oriData, unsigned char* d_cmpBytes,
     cudaMemcpy(&glob_sync, d_cmpOffset+cmpOffSize-1, sizeof(unsigned int), cudaMemcpyDeviceToHost);
     *cmpSize = (size_t)glob_sync + blockNum;
 
-    // Free memory that is used.
-    cudaFree(d_cmpOffset);
-    cudaFree(d_locOffset);
-    cudaFree(d_flag);
 } 
  
 /** ************************************************************************
@@ -274,14 +278,12 @@ void cuSZp_decompress_2D_outlier_f32(float* d_decData, unsigned char* d_cmpBytes
     int cmpOffSize = gsize + 1;
 
     // Initializing global memory for GPU compression.
-    unsigned int* d_cmpOffset;
-    unsigned int* d_locOffset;
-    int* d_flag;
-    cudaMalloc((void**)&d_cmpOffset, sizeof(unsigned int)*cmpOffSize);
+    g_scratch_cuSZp_decompress_2D_outlier_f32.ensure(cmpOffSize);
+    unsigned int* d_cmpOffset = g_scratch_cuSZp_decompress_2D_outlier_f32.d_cmpOffset;
+    unsigned int* d_locOffset = g_scratch_cuSZp_decompress_2D_outlier_f32.d_locOffset;
+    int* d_flag = g_scratch_cuSZp_decompress_2D_outlier_f32.d_flag;
     cudaMemset(d_cmpOffset, 0, sizeof(unsigned int)*cmpOffSize);
-    cudaMalloc((void**)&d_locOffset, sizeof(unsigned int)*cmpOffSize);
     cudaMemset(d_locOffset, 0, sizeof(unsigned int)*cmpOffSize);
-    cudaMalloc((void**)&d_flag, sizeof(int)*cmpOffSize);
     cudaMemset(d_flag, 0, sizeof(int)*cmpOffSize);
 
     // Decompression (implement for now, will update later with adaptive vectorization)
@@ -289,8 +291,4 @@ void cuSZp_decompress_2D_outlier_f32(float* d_decData, unsigned char* d_cmpBytes
     dim3 gridSize(gsize);
     cuSZp_decompress_kernel_2D_outlier_vec4_f32<<<gridSize, blockSize, sizeof(unsigned int)*2, stream>>>(d_decData, d_cmpBytes, d_cmpOffset, d_locOffset, d_flag, blockNum, dims, errorBound);
 
-    // Free memory that is used.
-    cudaFree(d_cmpOffset);
-    cudaFree(d_locOffset);
-    cudaFree(d_flag);
 }
\ No newline at end of file
diff --git a/src/cuSZp_entry_2D_f64.cu b/src/cuSZp_entry_2D_f64.cu
index 749fbb2..d4a4d18 100644
--- a/src/cuSZp_entry_2D_f64.cu
+++ b/src/cuSZp_entry_2D_f64.cu
@@ -1,6 +1,40 @@
 #include "cuSZp_entry_2D_f64.h"
 #include "cuSZp_kernels_2D_f64.h"
 
+// Scratch-buffer cache: cuSZp originally cudaMalloc'd + cudaFree'd 3 small scratch
+// arrays (d_cmpOffset/d_locOffset/d_flag) on every compress/decompress call. Cheap
+// on bare-metal GPUs, but on some platforms (observed on a GPU-passthrough cloud
+// VM) cudaMalloc/cudaFree carry large, highly variable latency (seen up to several
+// hundred ms for a single call) that dwarfs actual kernel time and dominates any
+// per-call timing. One cache per function below, grow-allocated once per distinct
+// cmpOffSize and reused across calls (still memset to 0 every call — correctness
+// unaffected). Freed only at process exit, fine for this CLI.
+namespace {
+struct CuszpScratch {
+    unsigned int* d_cmpOffset = nullptr;
+    unsigned int* d_locOffset = nullptr;
+    int* d_flag = nullptr;
+    size_t capacity = 0;
+
+    void ensure(size_t cmpOffSize) {
+        if (cmpOffSize <= capacity) return;
+        if (d_cmpOffset) cudaFree(d_cmpOffset);
+        if (d_locOffset) cudaFree(d_locOffset);
+        if (d_flag) cudaFree(d_flag);
+        cudaMalloc((void**)&d_cmpOffset, sizeof(unsigned int) * cmpOffSize);
+        cudaMalloc((void**)&d_locOffset, sizeof(unsigned int) * cmpOffSize);
+        cudaMalloc((void**)&d_flag, sizeof(int) * cmpOffSize);
+        capacity = cmpOffSize;
+    }
+};
+CuszpScratch g_scratch_cuSZp_compress_2D_fixed_f64;
+CuszpScratch g_scratch_cuSZp_decompress_2D_fixed_f64;
+CuszpScratch g_scratch_cuSZp_compress_2D_plain_f64;
+CuszpScratch g_scratch_cuSZp_decompress_2D_plain_f64;
+CuszpScratch g_scratch_cuSZp_compress_2D_outlier_f64;
+CuszpScratch g_scratch_cuSZp_decompress_2D_outlier_f64;
+}  // namespace
+
 /** ************************************************************************
  * @brief cuSZp end-to-end compression API for device pointers
  *        Compression is executed in GPU.
@@ -26,15 +60,13 @@ void cuSZp_compress_2D_fixed_f64(double* d_oriData, unsigned char* d_cmpBytes, s
     int cmpOffSize = gsize + 1;
 
     // Initializing global memory for GPU compression.
-    unsigned int* d_cmpOffset;
-    unsigned int* d_locOffset;
-    int* d_flag;
+    g_scratch_cuSZp_compress_2D_fixed_f64.ensure(cmpOffSize);
+    unsigned int* d_cmpOffset = g_scratch_cuSZp_compress_2D_fixed_f64.d_cmpOffset;
+    unsigned int* d_locOffset = g_scratch_cuSZp_compress_2D_fixed_f64.d_locOffset;
+    int* d_flag = g_scratch_cuSZp_compress_2D_fixed_f64.d_flag;
     unsigned int glob_sync;
-    cudaMalloc((void**)&d_cmpOffset, sizeof(unsigned int)*cmpOffSize);
     cudaMemset(d_cmpOffset, 0, sizeof(unsigned int)*cmpOffSize);
-    cudaMalloc((void**)&d_locOffset, sizeof(unsigned int)*cmpOffSize);
     cudaMemset(d_locOffset, 0, sizeof(unsigned int)*cmpOffSize);
-    cudaMalloc((void**)&d_flag, sizeof(int)*cmpOffSize);
     cudaMemset(d_flag, 0, sizeof(int)*cmpOffSize);
 
     // Compression (implement for now, will update later)
@@ -46,10 +78,6 @@ void cuSZp_compress_2D_fixed_f64(double* d_oriData, unsigned char* d_cmpBytes, s
     cudaMemcpy(&glob_sync, d_cmpOffset+cmpOffSize-1, sizeof(unsigned int), cudaMemcpyDeviceToHost);
     *cmpSize = (size_t)glob_sync + blockNum;
 
-    // Free memory that is used.
-    cudaFree(d_cmpOffset);
-    cudaFree(d_locOffset);
-    cudaFree(d_flag);
 } 
 
 /** ************************************************************************
@@ -78,14 +106,12 @@ void cuSZp_decompress_2D_fixed_f64(double* d_decData, unsigned char* d_cmpBytes,
     int cmpOffSize = gsize + 1;
 
     // Initializing global memory for GPU compression.
-    unsigned int* d_cmpOffset;
-    unsigned int* d_locOffset;
-    int* d_flag;
-    cudaMalloc((void**)&d_cmpOffset, sizeof(unsigned int)*cmpOffSize);
+    g_scratch_cuSZp_decompress_2D_fixed_f64.ensure(cmpOffSize);
+    unsigned int* d_cmpOffset = g_scratch_cuSZp_decompress_2D_fixed_f64.d_cmpOffset;
+    unsigned int* d_locOffset = g_scratch_cuSZp_decompress_2D_fixed_f64.d_locOffset;
+    int* d_flag = g_scratch_cuSZp_decompress_2D_fixed_f64.d_flag;
     cudaMemset(d_cmpOffset, 0, sizeof(unsigned int)*cmpOffSize);
-    cudaMalloc((void**)&d_locOffset, sizeof(unsigned int)*cmpOffSize);
     cudaMemset(d_locOffset, 0, sizeof(unsigned int)*cmpOffSize);
-    cudaMalloc((void**)&d_flag, sizeof(int)*cmpOffSize);
     cudaMemset(d_flag, 0, sizeof(int)*cmpOffSize);
 
     // Decompression (implement for now, will update later with adaptive vectorization)
@@ -93,10 +119,6 @@ void cuSZp_decompress_2D_fixed_f64(double* d_decData, unsigned char* d_cmpBytes,
     dim3 gridSize(gsize);
     cuSZp_decompress_kernel_2D_fixed_vec4_f64<<<gridSize, blockSize, sizeof(unsigned int)*2, stream>>>(d_decData, d_cmpBytes, d_cmpOffset, d_locOffset, d_flag, blockNum, dims, errorBound);
 
-    // Free memory that is used.
-    cudaFree(d_cmpOffset);
-    cudaFree(d_locOffset);
-    cudaFree(d_flag);
 }
 
 /** ************************************************************************
@@ -124,15 +146,13 @@ void cuSZp_compress_2D_plain_f64(double* d_oriData, unsigned char* d_cmpBytes, s
     int cmpOffSize = gsize + 1;
 
     // Initializing global memory for GPU compression.
-    unsigned int* d_cmpOffset;
-    unsigned int* d_locOffset;
-    int* d_flag;
+    g_scratch_cuSZp_compress_2D_plain_f64.ensure(cmpOffSize);
+    unsigned int* d_cmpOffset = g_scratch_cuSZp_compress_2D_plain_f64.d_cmpOffset;
+    unsigned int* d_locOffset = g_scratch_cuSZp_compress_2D_plain_f64.d_locOffset;
+    int* d_flag = g_scratch_cuSZp_compress_2D_plain_f64.d_flag;
     unsigned int glob_sync;
-    cudaMalloc((void**)&d_cmpOffset, sizeof(unsigned int)*cmpOffSize);
     cudaMemset(d_cmpOffset, 0, sizeof(unsigned int)*cmpOffSize);
-    cudaMalloc((void**)&d_locOffset, sizeof(unsigned int)*cmpOffSize);
     cudaMemset(d_locOffset, 0, sizeof(unsigned int)*cmpOffSize);
-    cudaMalloc((void**)&d_flag, sizeof(int)*cmpOffSize);
     cudaMemset(d_flag, 0, sizeof(int)*cmpOffSize);
 
     // Compression (implement for now, will update later)
@@ -144,10 +164,6 @@ void cuSZp_compress_2D_plain_f64(double* d_oriData, unsigned char* d_cmpBytes, s
     cudaMemcpy(&glob_sync, d_cmpOffset+cmpOffSize-1, sizeof(unsigned int), cudaMemcpyDeviceToHost);
     *cmpSize = (size_t)glob_sync + blockNum;
 
-    // Free memory that is used.
-    cudaFree(d_cmpOffset);
-    cudaFree(d_locOffset);
-    cudaFree(d_flag);
 } 
 
 /** ************************************************************************
@@ -176,14 +192,12 @@ void cuSZp_decompress_2D_plain_f64(double* d_decData, unsigned char* d_cmpBytes,
     int cmpOffSize = gsize + 1;
 
     // Initializing global memory for GPU compression.
-    unsigned int* d_cmpOffset;
-    unsigned int* d_locOffset;
-    int* d_flag;
-    cudaMalloc((void**)&d_cmpOffset, sizeof(unsigned int)*cmpOffSize);
+    g_scratch_cuSZp_decompress_2D_plain_f64.ensure(cmpOffSize);
+    unsigned int* d_cmpOffset = g_scratch_cuSZp_decompress_2D_plain_f64.d_cmpOffset;
+    unsigned int* d_locOffset = g_scratch_cuSZp_decompress_2D_plain_f64.d_locOffset;
+    int* d_flag = g_scratch_cuSZp_decompress_2D_plain_f64.d_flag;
     cudaMemset(d_cmpOffset, 0, sizeof(unsigned int)*cmpOffSize);
-    cudaMalloc((void**)&d_locOffset, sizeof(unsigned int)*cmpOffSize);
     cudaMemset(d_locOffset, 0, sizeof(unsigned int)*cmpOffSize);
-    cudaMalloc((void**)&d_flag, sizeof(int)*cmpOffSize);
     cudaMemset(d_flag, 0, sizeof(int)*cmpOffSize);
 
     // Decompression (implement for now, will update later with adaptive vectorization)
@@ -191,10 +205,6 @@ void cuSZp_decompress_2D_plain_f64(double* d_decData, unsigned char* d_cmpBytes,
     dim3 gridSize(gsize);
     cuSZp_decompress_kernel_2D_plain_vec4_f64<<<gridSize, blockSize, sizeof(unsigned int)*2, stream>>>(d_decData, d_cmpBytes, d_cmpOffset, d_locOffset, d_flag, blockNum, dims, errorBound);
 
-    // Free memory that is used.
-    cudaFree(d_cmpOffset);
-    cudaFree(d_locOffset);
-    cudaFree(d_flag);
 }
 
 /** ************************************************************************
@@ -222,15 +232,13 @@ void cuSZp_compress_2D_outlier_f64(double* d_oriData, unsigned char* d_cmpBytes,
     int cmpOffSize = gsize + 1;
 
     // Initializing global memory for GPU compression.
-    unsigned int* d_cmpOffset;
-    unsigned int* d_locOffset;
-    int* d_flag;
+    g_scratch_cuSZp_compress_2D_outlier_f64.ensure(cmpOffSize);
+    unsigned int* d_cmpOffset = g_scratch_cuSZp_compress_2D_outlier_f64.d_cmpOffset;
+    unsigned int* d_locOffset = g_scratch_cuSZp_compress_2D_outlier_f64.d_locOffset;
+    int* d_flag = g_scratch_cuSZp_compress_2D_outlier_f64.d_flag;
     unsigned int glob_sync;
-    cudaMalloc((void**)&d_cmpOffset, sizeof(unsigned int)*cmpOffSize);
     cudaMemset(d_cmpOffset, 0, sizeof(unsigned int)*cmpOffSize);
-    cudaMalloc((void**)&d_locOffset, sizeof(unsigned int)*cmpOffSize);
     cudaMemset(d_locOffset, 0, sizeof(unsigned int)*cmpOffSize);
-    cudaMalloc((void**)&d_flag, sizeof(int)*cmpOffSize);
     cudaMemset(d_flag, 0, sizeof(int)*cmpOffSize);
 
     // Compression (implement for now, will update later)
@@ -242,10 +250,6 @@ void cuSZp_compress_2D_outlier_f64(double* d_oriData, unsigned char* d_cmpBytes,
     cudaMemcpy(&glob_sync, d_cmpOffset+cmpOffSize-1, sizeof(unsigned int), cudaMemcpyDeviceToHost);
     *cmpSize = (size_t)glob_sync + blockNum;
 
-    // Free memory that is used.
-    cudaFree(d_cmpOffset);
-    cudaFree(d_locOffset);
-    cudaFree(d_flag);
 } 
  
 /** ************************************************************************
@@ -274,14 +278,12 @@ void cuSZp_decompress_2D_outlier_f64(double* d_decData, unsigned char* d_cmpByte
     int cmpOffSize = gsize + 1;
 
     // Initializing global memory for GPU compression.
-    unsigned int* d_cmpOffset;
-    unsigned int* d_locOffset;
-    int* d_flag;
-    cudaMalloc((void**)&d_cmpOffset, sizeof(unsigned int)*cmpOffSize);
+    g_scratch_cuSZp_decompress_2D_outlier_f64.ensure(cmpOffSize);
+    unsigned int* d_cmpOffset = g_scratch_cuSZp_decompress_2D_outlier_f64.d_cmpOffset;
+    unsigned int* d_locOffset = g_scratch_cuSZp_decompress_2D_outlier_f64.d_locOffset;
+    int* d_flag = g_scratch_cuSZp_decompress_2D_outlier_f64.d_flag;
     cudaMemset(d_cmpOffset, 0, sizeof(unsigned int)*cmpOffSize);
-    cudaMalloc((void**)&d_locOffset, sizeof(unsigned int)*cmpOffSize);
     cudaMemset(d_locOffset, 0, sizeof(unsigned int)*cmpOffSize);
-    cudaMalloc((void**)&d_flag, sizeof(int)*cmpOffSize);
     cudaMemset(d_flag, 0, sizeof(int)*cmpOffSize);
 
     // Decompression (implement for now, will update later with adaptive vectorization)
@@ -289,8 +291,4 @@ void cuSZp_decompress_2D_outlier_f64(double* d_decData, unsigned char* d_cmpByte
     dim3 gridSize(gsize);
     cuSZp_decompress_kernel_2D_outlier_vec4_f64<<<gridSize, blockSize, sizeof(unsigned int)*2, stream>>>(d_decData, d_cmpBytes, d_cmpOffset, d_locOffset, d_flag, blockNum, dims, errorBound);
 
-    // Free memory that is used.
-    cudaFree(d_cmpOffset);
-    cudaFree(d_locOffset);
-    cudaFree(d_flag);
 }
\ No newline at end of file
diff --git a/src/cuSZp_entry_3D_f32.cu b/src/cuSZp_entry_3D_f32.cu
index 70f99c0..116bc4f 100644
--- a/src/cuSZp_entry_3D_f32.cu
+++ b/src/cuSZp_entry_3D_f32.cu
@@ -1,6 +1,40 @@
 #include "cuSZp_entry_3D_f32.h"
 #include "cuSZp_kernels_3D_f32.h"
 
+// Scratch-buffer cache: cuSZp originally cudaMalloc'd + cudaFree'd 3 small scratch
+// arrays (d_cmpOffset/d_locOffset/d_flag) on every compress/decompress call. Cheap
+// on bare-metal GPUs, but on some platforms (observed on a GPU-passthrough cloud
+// VM) cudaMalloc/cudaFree carry large, highly variable latency (seen up to several
+// hundred ms for a single call) that dwarfs actual kernel time and dominates any
+// per-call timing. One cache per function below, grow-allocated once per distinct
+// cmpOffSize and reused across calls (still memset to 0 every call — correctness
+// unaffected). Freed only at process exit, fine for this CLI.
+namespace {
+struct CuszpScratch {
+    unsigned int* d_cmpOffset = nullptr;
+    unsigned int* d_locOffset = nullptr;
+    int* d_flag = nullptr;
+    size_t capacity = 0;
+
+    void ensure(size_t cmpOffSize) {
+        if (cmpOffSize <= capacity) return;
+        if (d_cmpOffset) cudaFree(d_cmpOffset);
+        if (d_locOffset) cudaFree(d_locOffset);
+        if (d_flag) cudaFree(d_flag);
+        cudaMalloc((void**)&d_cmpOffset, sizeof(unsigned int) * cmpOffSize);
+        cudaMalloc((void**)&d_locOffset, sizeof(unsigned int) * cmpOffSize);
+        cudaMalloc((void**)&d_flag, sizeof(int) * cmpOffSize);
+        capacity = cmpOffSize;
+    }
+};
+CuszpScratch g_scratch_cuSZp_compress_3D_fixed_f32;
+CuszpScratch g_scratch_cuSZp_decompress_3D_fixed_f32;
+CuszpScratch g_scratch_cuSZp_compress_3D_plain_f32;
+CuszpScratch g_scratch_cuSZp_decompress_3D_plain_f32;
+CuszpScratch g_scratch_cuSZp_compress_3D_outlier_f32;
+CuszpScratch g_scratch_cuSZp_decompress_3D_outlier_f32;
+}  // namespace
+
 /** ************************************************************************
 * @brief cuSZp end-to-end compression API for device pointers
 *        Compression is executed in GPU.
@@ -27,15 +61,13 @@ void cuSZp_compress_3D_fixed_f32(float* d_oriData, unsigned char* d_cmpBytes, si
     int cmpOffSize = gsize + 1;
 
     // Initializing global memory for GPU compression.
-    unsigned int* d_cmpOffset;
-    unsigned int* d_locOffset;
-    int* d_flag;
+    g_scratch_cuSZp_compress_3D_fixed_f32.ensure(cmpOffSize);
+    unsigned int* d_cmpOffset = g_scratch_cuSZp_compress_3D_fixed_f32.d_cmpOffset;
+    unsigned int* d_locOffset = g_scratch_cuSZp_compress_3D_fixed_f32.d_locOffset;
+    int* d_flag = g_scratch_cuSZp_compress_3D_fixed_f32.d_flag;
     unsigned int glob_sync;
-    cudaMalloc((void**)&d_cmpOffset, sizeof(unsigned int)*cmpOffSize);
     cudaMemset(d_cmpOffset, 0, sizeof(unsigned int)*cmpOffSize);
-    cudaMalloc((void**)&d_locOffset, sizeof(unsigned int)*cmpOffSize);
     cudaMemset(d_locOffset, 0, sizeof(unsigned int)*cmpOffSize);
-    cudaMalloc((void**)&d_flag, sizeof(int)*cmpOffSize);
     cudaMemset(d_flag, 0, sizeof(int)*cmpOffSize);
 
     // Compression (implement for now, will update later)
@@ -47,10 +79,6 @@ void cuSZp_compress_3D_fixed_f32(float* d_oriData, unsigned char* d_cmpBytes, si
     cudaMemcpy(&glob_sync, d_cmpOffset+cmpOffSize-1, sizeof(unsigned int), cudaMemcpyDeviceToHost);
     *cmpSize = (size_t)glob_sync + blockNum;
 
-    // Free memory that is used.
-    cudaFree(d_cmpOffset);
-    cudaFree(d_locOffset);
-    cudaFree(d_flag);
 } 
  
 /** ************************************************************************
@@ -80,14 +108,12 @@ void cuSZp_decompress_3D_fixed_f32(float* d_decData, unsigned char* d_cmpBytes,
     int cmpOffSize = gsize + 1;
 
     // Initializing global memory for GPU compression.
-    unsigned int* d_cmpOffset;
-    unsigned int* d_locOffset;
-    int* d_flag;
-    cudaMalloc((void**)&d_cmpOffset, sizeof(unsigned int)*cmpOffSize);
+    g_scratch_cuSZp_decompress_3D_fixed_f32.ensure(cmpOffSize);
+    unsigned int* d_cmpOffset = g_scratch_cuSZp_decompress_3D_fixed_f32.d_cmpOffset;
+    unsigned int* d_locOffset = g_scratch_cuSZp_decompress_3D_fixed_f32.d_locOffset;
+    int* d_flag = g_scratch_cuSZp_decompress_3D_fixed_f32.d_flag;
     cudaMemset(d_cmpOffset, 0, sizeof(unsigned int)*cmpOffSize);
-    cudaMalloc((void**)&d_locOffset, sizeof(unsigned int)*cmpOffSize);
     cudaMemset(d_locOffset, 0, sizeof(unsigned int)*cmpOffSize);
-    cudaMalloc((void**)&d_flag, sizeof(int)*cmpOffSize);
     cudaMemset(d_flag, 0, sizeof(int)*cmpOffSize);
 
     // Decompression (implement for now, will update later with adaptive vectorization)
@@ -95,10 +121,6 @@ void cuSZp_decompress_3D_fixed_f32(float* d_decData, unsigned char* d_cmpBytes,
     dim3 gridSize(gsize);
     cuSZp_decompress_kernel_3D_fixed_vec4_f32<<<gridSize, blockSize, sizeof(unsigned int)*2, stream>>>(d_decData, d_cmpBytes, d_cmpOffset, d_locOffset, d_flag, blockNum, dims, errorBound);
 
-    // Free memory that is used.
-    cudaFree(d_cmpOffset);
-    cudaFree(d_locOffset);
-    cudaFree(d_flag);
 }
 
 /** ************************************************************************
@@ -127,15 +149,13 @@ void cuSZp_compress_3D_plain_f32(float* d_oriData, unsigned char* d_cmpBytes, si
     int cmpOffSize = gsize + 1;
 
     // Initializing global memory for GPU compression.
-    unsigned int* d_cmpOffset;
-    unsigned int* d_locOffset;
-    int* d_flag;
+    g_scratch_cuSZp_compress_3D_plain_f32.ensure(cmpOffSize);
+    unsigned int* d_cmpOffset = g_scratch_cuSZp_compress_3D_plain_f32.d_cmpOffset;
+    unsigned int* d_locOffset = g_scratch_cuSZp_compress_3D_plain_f32.d_locOffset;
+    int* d_flag = g_scratch_cuSZp_compress_3D_plain_f32.d_flag;
     unsigned int glob_sync;
-    cudaMalloc((void**)&d_cmpOffset, sizeof(unsigned int)*cmpOffSize);
     cudaMemset(d_cmpOffset, 0, sizeof(unsigned int)*cmpOffSize);
-    cudaMalloc((void**)&d_locOffset, sizeof(unsigned int)*cmpOffSize);
     cudaMemset(d_locOffset, 0, sizeof(unsigned int)*cmpOffSize);
-    cudaMalloc((void**)&d_flag, sizeof(int)*cmpOffSize);
     cudaMemset(d_flag, 0, sizeof(int)*cmpOffSize);
 
     // Compression (implement for now, will update later)
@@ -147,10 +167,6 @@ void cuSZp_compress_3D_plain_f32(float* d_oriData, unsigned char* d_cmpBytes, si
     cudaMemcpy(&glob_sync, d_cmpOffset+cmpOffSize-1, sizeof(unsigned int), cudaMemcpyDeviceToHost);
     *cmpSize = (size_t)glob_sync + blockNum;
 
-    // Free memory that is used.
-    cudaFree(d_cmpOffset);
-    cudaFree(d_locOffset);
-    cudaFree(d_flag);
 } 
  
 /** ************************************************************************
@@ -180,14 +196,12 @@ void cuSZp_decompress_3D_plain_f32(float* d_decData, unsigned char* d_cmpBytes,
     int cmpOffSize = gsize + 1;
 
     // Initializing global memory for GPU compression.
-    unsigned int* d_cmpOffset;
-    unsigned int* d_locOffset;
-    int* d_flag;
-    cudaMalloc((void**)&d_cmpOffset, sizeof(unsigned int)*cmpOffSize);
+    g_scratch_cuSZp_decompress_3D_plain_f32.ensure(cmpOffSize);
+    unsigned int* d_cmpOffset = g_scratch_cuSZp_decompress_3D_plain_f32.d_cmpOffset;
+    unsigned int* d_locOffset = g_scratch_cuSZp_decompress_3D_plain_f32.d_locOffset;
+    int* d_flag = g_scratch_cuSZp_decompress_3D_plain_f32.d_flag;
     cudaMemset(d_cmpOffset, 0, sizeof(unsigned int)*cmpOffSize);
-    cudaMalloc((void**)&d_locOffset, sizeof(unsigned int)*cmpOffSize);
     cudaMemset(d_locOffset, 0, sizeof(unsigned int)*cmpOffSize);
-    cudaMalloc((void**)&d_flag, sizeof(int)*cmpOffSize);
     cudaMemset(d_flag, 0, sizeof(int)*cmpOffSize);
 
     // Decompression (implement for now, will update later with adaptive vectorization)
@@ -195,10 +209,6 @@ void cuSZp_decompress_3D_plain_f32(float* d_decData, unsigned char* d_cmpBytes,
     dim3 gridSize(gsize);
     cuSZp_decompress_kernel_3D_plain_vec4_f32<<<gridSize, blockSize, sizeof(unsigned int)*2, stream>>>(d_decData, d_cmpBytes, d_cmpOffset, d_locOffset, d_flag, blockNum, dims, errorBound);
 
-    // Free memory that is used.
-    cudaFree(d_cmpOffset);
-    cudaFree(d_locOffset);
-    cudaFree(d_flag);
 }
 
 /** ************************************************************************
@@ -227,15 +237,13 @@ void cuSZp_compress_3D_outlier_f32(float* d_oriData, unsigned char* d_cmpBytes,
     int cmpOffSize = gsize + 1;
 
     // Initializing global memory for GPU compression.
-    unsigned int* d_cmpOffset;
-    unsigned int* d_locOffset;
-    int* d_flag;
+    g_scratch_cuSZp_compress_3D_outlier_f32.ensure(cmpOffSize);
+    unsigned int* d_cmpOffset = g_scratch_cuSZp_compress_3D_outlier_f32.d_cmpOffset;
+    unsigned int* d_locOffset = g_scratch_cuSZp_compress_3D_outlier_f32.d_locOffset;
+    int* d_flag = g_scratch_cuSZp_compress_3D_outlier_f32.d_flag;
     unsigned int glob_sync;
-    cudaMalloc((void**)&d_cmpOffset, sizeof(unsigned int)*cmpOffSize);
     cudaMemset(d_cmpOffset, 0, sizeof(unsigned int)*cmpOffSize);
-    cudaMalloc((void**)&d_locOffset, sizeof(unsigned int)*cmpOffSize);
     cudaMemset(d_locOffset, 0, sizeof(unsigned int)*cmpOffSize);
-    cudaMalloc((void**)&d_flag, sizeof(int)*cmpOffSize);
     cudaMemset(d_flag, 0, sizeof(int)*cmpOffSize);
 
     // Compression (implement for now, will update later)
@@ -247,10 +255,6 @@ void cuSZp_compress_3D_outlier_f32(float* d_oriData, unsigned char* d_cmpBytes,
     cudaMemcpy(&glob_sync, d_cmpOffset+cmpOffSize-1, sizeof(unsigned int), cudaMemcpyDeviceToHost);
     *cmpSize = (size_t)glob_sync + blockNum;
 
-    // Free memory that is used.
-    cudaFree(d_cmpOffset);
-    cudaFree(d_locOffset);
-    cudaFree(d_flag);
 } 
  
 /** ************************************************************************
@@ -280,14 +284,12 @@ void cuSZp_decompress_3D_outlier_f32(float* d_decData, unsigned char* d_cmpBytes
     int cmpOffSize = gsize + 1;
 
     // Initializing global memory for GPU compression.
-    unsigned int* d_cmpOffset;
-    unsigned int* d_locOffset;
-    int* d_flag;
-    cudaMalloc((void**)&d_cmpOffset, sizeof(unsigned int)*cmpOffSize);
+    g_scratch_cuSZp_decompress_3D_outlier_f32.ensure(cmpOffSize);
+    unsigned int* d_cmpOffset = g_scratch_cuSZp_decompress_3D_outlier_f32.d_cmpOffset;
+    unsigned int* d_locOffset = g_scratch_cuSZp_decompress_3D_outlier_f32.d_locOffset;
+    int* d_flag = g_scratch_cuSZp_decompress_3D_outlier_f32.d_flag;
     cudaMemset(d_cmpOffset, 0, sizeof(unsigned int)*cmpOffSize);
-    cudaMalloc((void**)&d_locOffset, sizeof(unsigned int)*cmpOffSize);
     cudaMemset(d_locOffset, 0, sizeof(unsigned int)*cmpOffSize);
-    cudaMalloc((void**)&d_flag, sizeof(int)*cmpOffSize);
     cudaMemset(d_flag, 0, sizeof(int)*cmpOffSize);
 
     // Decompression (implement for now, will update later with adaptive vectorization)
@@ -295,8 +297,4 @@ void cuSZp_decompress_3D_outlier_f32(float* d_decData, unsigned char* d_cmpBytes
     dim3 gridSize(gsize);
     cuSZp_decompress_kernel_3D_outlier_vec4_f32<<<gridSize, blockSize, sizeof(unsigned int)*2, stream>>>(d_decData, d_cmpBytes, d_cmpOffset, d_locOffset, d_flag, blockNum, dims, errorBound);
 
-    // Free memory that is used.
-    cudaFree(d_cmpOffset);
-    cudaFree(d_locOffset);
-    cudaFree(d_flag);
 }
\ No newline at end of file
diff --git a/src/cuSZp_entry_3D_f64.cu b/src/cuSZp_entry_3D_f64.cu
index 6ac5ffe..a8808e7 100644
--- a/src/cuSZp_entry_3D_f64.cu
+++ b/src/cuSZp_entry_3D_f64.cu
@@ -1,6 +1,40 @@
 #include "cuSZp_entry_3D_f64.h"
 #include "cuSZp_kernels_3D_f64.h"
 
+// Scratch-buffer cache: cuSZp originally cudaMalloc'd + cudaFree'd 3 small scratch
+// arrays (d_cmpOffset/d_locOffset/d_flag) on every compress/decompress call. Cheap
+// on bare-metal GPUs, but on some platforms (observed on a GPU-passthrough cloud
+// VM) cudaMalloc/cudaFree carry large, highly variable latency (seen up to several
+// hundred ms for a single call) that dwarfs actual kernel time and dominates any
+// per-call timing. One cache per function below, grow-allocated once per distinct
+// cmpOffSize and reused across calls (still memset to 0 every call — correctness
+// unaffected). Freed only at process exit, fine for this CLI.
+namespace {
+struct CuszpScratch {
+    unsigned int* d_cmpOffset = nullptr;
+    unsigned int* d_locOffset = nullptr;
+    int* d_flag = nullptr;
+    size_t capacity = 0;
+
+    void ensure(size_t cmpOffSize) {
+        if (cmpOffSize <= capacity) return;
+        if (d_cmpOffset) cudaFree(d_cmpOffset);
+        if (d_locOffset) cudaFree(d_locOffset);
+        if (d_flag) cudaFree(d_flag);
+        cudaMalloc((void**)&d_cmpOffset, sizeof(unsigned int) * cmpOffSize);
+        cudaMalloc((void**)&d_locOffset, sizeof(unsigned int) * cmpOffSize);
+        cudaMalloc((void**)&d_flag, sizeof(int) * cmpOffSize);
+        capacity = cmpOffSize;
+    }
+};
+CuszpScratch g_scratch_cuSZp_compress_3D_fixed_f64;
+CuszpScratch g_scratch_cuSZp_decompress_3D_fixed_f64;
+CuszpScratch g_scratch_cuSZp_compress_3D_plain_f64;
+CuszpScratch g_scratch_cuSZp_decompress_3D_plain_f64;
+CuszpScratch g_scratch_cuSZp_compress_3D_outlier_f64;
+CuszpScratch g_scratch_cuSZp_decompress_3D_outlier_f64;
+}  // namespace
+
 /** ************************************************************************
 * @brief cuSZp end-to-end compression API for device pointers
 *        Compression is executed in GPU.
@@ -27,15 +61,13 @@ void cuSZp_compress_3D_fixed_f64(double* d_oriData, unsigned char* d_cmpBytes, s
     int cmpOffSize = gsize + 1;
 
     // Initializing global memory for GPU compression.
-    unsigned int* d_cmpOffset;
-    unsigned int* d_locOffset;
-    int* d_flag;
+    g_scratch_cuSZp_compress_3D_fixed_f64.ensure(cmpOffSize);
+    unsigned int* d_cmpOffset = g_scratch_cuSZp_compress_3D_fixed_f64.d_cmpOffset;
+    unsigned int* d_locOffset = g_scratch_cuSZp_compress_3D_fixed_f64.d_locOffset;
+    int* d_flag = g_scratch_cuSZp_compress_3D_fixed_f64.d_flag;
     unsigned int glob_sync;
-    cudaMalloc((void**)&d_cmpOffset, sizeof(unsigned int)*cmpOffSize);
     cudaMemset(d_cmpOffset, 0, sizeof(unsigned int)*cmpOffSize);
-    cudaMalloc((void**)&d_locOffset, sizeof(unsigned int)*cmpOffSize);
     cudaMemset(d_locOffset, 0, sizeof(unsigned int)*cmpOffSize);
-    cudaMalloc((void**)&d_flag, sizeof(int)*cmpOffSize);
     cudaMemset(d_flag, 0, sizeof(int)*cmpOffSize);
 
     // Compression (implement for now, will update later)
@@ -47,10 +79,6 @@ void cuSZp_compress_3D_fixed_f64(double* d_oriData, unsigned char* d_cmpBytes, s
     cudaMemcpy(&glob_sync, d_cmpOffset+cmpOffSize-1, sizeof(unsigned int), cudaMemcpyDeviceToHost);
     *cmpSize = (size_t)glob_sync + blockNum;
 
-    // Free memory that is used.
-    cudaFree(d_cmpOffset);
-    cudaFree(d_locOffset);
-    cudaFree(d_flag);
 } 
  
 /** ************************************************************************
@@ -80,14 +108,12 @@ void cuSZp_decompress_3D_fixed_f64(double* d_decData, unsigned char* d_cmpBytes,
     int cmpOffSize = gsize + 1;
 
     // Initializing global memory for GPU compression.
-    unsigned int* d_cmpOffset;
-    unsigned int* d_locOffset;
-    int* d_flag;
-    cudaMalloc((void**)&d_cmpOffset, sizeof(unsigned int)*cmpOffSize);
+    g_scratch_cuSZp_decompress_3D_fixed_f64.ensure(cmpOffSize);
+    unsigned int* d_cmpOffset = g_scratch_cuSZp_decompress_3D_fixed_f64.d_cmpOffset;
+    unsigned int* d_locOffset = g_scratch_cuSZp_decompress_3D_fixed_f64.d_locOffset;
+    int* d_flag = g_scratch_cuSZp_decompress_3D_fixed_f64.d_flag;
     cudaMemset(d_cmpOffset, 0, sizeof(unsigned int)*cmpOffSize);
-    cudaMalloc((void**)&d_locOffset, sizeof(unsigned int)*cmpOffSize);
     cudaMemset(d_locOffset, 0, sizeof(unsigned int)*cmpOffSize);
-    cudaMalloc((void**)&d_flag, sizeof(int)*cmpOffSize);
     cudaMemset(d_flag, 0, sizeof(int)*cmpOffSize);
 
     // Decompression (implement for now, will update later with adaptive vectorization)
@@ -95,10 +121,6 @@ void cuSZp_decompress_3D_fixed_f64(double* d_decData, unsigned char* d_cmpBytes,
     dim3 gridSize(gsize);
     cuSZp_decompress_kernel_3D_fixed_vec4_f64<<<gridSize, blockSize, sizeof(unsigned int)*2, stream>>>(d_decData, d_cmpBytes, d_cmpOffset, d_locOffset, d_flag, blockNum, dims, errorBound);
 
-    // Free memory that is used.
-    cudaFree(d_cmpOffset);
-    cudaFree(d_locOffset);
-    cudaFree(d_flag);
 }
 
 /** ************************************************************************
@@ -127,15 +149,13 @@ void cuSZp_compress_3D_plain_f64(double* d_oriData, unsigned char* d_cmpBytes, s
     int cmpOffSize = gsize + 1;
 
     // Initializing global memory for GPU compression.
-    unsigned int* d_cmpOffset;
-    unsigned int* d_locOffset;
-    int* d_flag;
+    g_scratch_cuSZp_compress_3D_plain_f64.ensure(cmpOffSize);
+    unsigned int* d_cmpOffset = g_scratch_cuSZp_compress_3D_plain_f64.d_cmpOffset;
+    unsigned int* d_locOffset = g_scratch_cuSZp_compress_3D_plain_f64.d_locOffset;
+    int* d_flag = g_scratch_cuSZp_compress_3D_plain_f64.d_flag;
     unsigned int glob_sync;
-    cudaMalloc((void**)&d_cmpOffset, sizeof(unsigned int)*cmpOffSize);
     cudaMemset(d_cmpOffset, 0, sizeof(unsigned int)*cmpOffSize);
-    cudaMalloc((void**)&d_locOffset, sizeof(unsigned int)*cmpOffSize);
     cudaMemset(d_locOffset, 0, sizeof(unsigned int)*cmpOffSize);
-    cudaMalloc((void**)&d_flag, sizeof(int)*cmpOffSize);
     cudaMemset(d_flag, 0, sizeof(int)*cmpOffSize);
 
     // Compression (implement for now, will update later)
@@ -147,10 +167,6 @@ void cuSZp_compress_3D_plain_f64(double* d_oriData, unsigned char* d_cmpBytes, s
     cudaMemcpy(&glob_sync, d_cmpOffset+cmpOffSize-1, sizeof(unsigned int), cudaMemcpyDeviceToHost);
     *cmpSize = (size_t)glob_sync + blockNum;
 
-    // Free memory that is used.
-    cudaFree(d_cmpOffset);
-    cudaFree(d_locOffset);
-    cudaFree(d_flag);
 } 
  
 /** ************************************************************************
@@ -180,14 +196,12 @@ void cuSZp_decompress_3D_plain_f64(double* d_decData, unsigned char* d_cmpBytes,
     int cmpOffSize = gsize + 1;
 
     // Initializing global memory for GPU compression.
-    unsigned int* d_cmpOffset;
-    unsigned int* d_locOffset;
-    int* d_flag;
-    cudaMalloc((void**)&d_cmpOffset, sizeof(unsigned int)*cmpOffSize);
+    g_scratch_cuSZp_decompress_3D_plain_f64.ensure(cmpOffSize);
+    unsigned int* d_cmpOffset = g_scratch_cuSZp_decompress_3D_plain_f64.d_cmpOffset;
+    unsigned int* d_locOffset = g_scratch_cuSZp_decompress_3D_plain_f64.d_locOffset;
+    int* d_flag = g_scratch_cuSZp_decompress_3D_plain_f64.d_flag;
     cudaMemset(d_cmpOffset, 0, sizeof(unsigned int)*cmpOffSize);
-    cudaMalloc((void**)&d_locOffset, sizeof(unsigned int)*cmpOffSize);
     cudaMemset(d_locOffset, 0, sizeof(unsigned int)*cmpOffSize);
-    cudaMalloc((void**)&d_flag, sizeof(int)*cmpOffSize);
     cudaMemset(d_flag, 0, sizeof(int)*cmpOffSize);
 
     // Decompression (implement for now, will update later with adaptive vectorization)
@@ -195,10 +209,6 @@ void cuSZp_decompress_3D_plain_f64(double* d_decData, unsigned char* d_cmpBytes,
     dim3 gridSize(gsize);
     cuSZp_decompress_kernel_3D_plain_vec4_f64<<<gridSize, blockSize, sizeof(unsigned int)*2, stream>>>(d_decData, d_cmpBytes, d_cmpOffset, d_locOffset, d_flag, blockNum, dims, errorBound);
 
-    // Free memory that is used.
-    cudaFree(d_cmpOffset);
-    cudaFree(d_locOffset);
-    cudaFree(d_flag);
 }
 
 /** ************************************************************************
@@ -227,15 +237,13 @@ void cuSZp_compress_3D_outlier_f64(double* d_oriData, unsigned char* d_cmpBytes,
     int cmpOffSize = gsize + 1;
 
     // Initializing global memory for GPU compression.
-    unsigned int* d_cmpOffset;
-    unsigned int* d_locOffset;
-    int* d_flag;
+    g_scratch_cuSZp_compress_3D_outlier_f64.ensure(cmpOffSize);
+    unsigned int* d_cmpOffset = g_scratch_cuSZp_compress_3D_outlier_f64.d_cmpOffset;
+    unsigned int* d_locOffset = g_scratch_cuSZp_compress_3D_outlier_f64.d_locOffset;
+    int* d_flag = g_scratch_cuSZp_compress_3D_outlier_f64.d_flag;
     unsigned int glob_sync;
-    cudaMalloc((void**)&d_cmpOffset, sizeof(unsigned int)*cmpOffSize);
     cudaMemset(d_cmpOffset, 0, sizeof(unsigned int)*cmpOffSize);
-    cudaMalloc((void**)&d_locOffset, sizeof(unsigned int)*cmpOffSize);
     cudaMemset(d_locOffset, 0, sizeof(unsigned int)*cmpOffSize);
-    cudaMalloc((void**)&d_flag, sizeof(int)*cmpOffSize);
     cudaMemset(d_flag, 0, sizeof(int)*cmpOffSize);
 
     // Compression (implement for now, will update later)
@@ -247,10 +255,6 @@ void cuSZp_compress_3D_outlier_f64(double* d_oriData, unsigned char* d_cmpBytes,
     cudaMemcpy(&glob_sync, d_cmpOffset+cmpOffSize-1, sizeof(unsigned int), cudaMemcpyDeviceToHost);
     *cmpSize = (size_t)glob_sync + blockNum;
 
-    // Free memory that is used.
-    cudaFree(d_cmpOffset);
-    cudaFree(d_locOffset);
-    cudaFree(d_flag);
 } 
  
 /** ************************************************************************
@@ -280,14 +284,12 @@ void cuSZp_decompress_3D_outlier_f64(double* d_decData, unsigned char* d_cmpByte
     int cmpOffSize = gsize + 1;
 
     // Initializing global memory for GPU compression.
-    unsigned int* d_cmpOffset;
-    unsigned int* d_locOffset;
-    int* d_flag;
-    cudaMalloc((void**)&d_cmpOffset, sizeof(unsigned int)*cmpOffSize);
+    g_scratch_cuSZp_decompress_3D_outlier_f64.ensure(cmpOffSize);
+    unsigned int* d_cmpOffset = g_scratch_cuSZp_decompress_3D_outlier_f64.d_cmpOffset;
+    unsigned int* d_locOffset = g_scratch_cuSZp_decompress_3D_outlier_f64.d_locOffset;
+    int* d_flag = g_scratch_cuSZp_decompress_3D_outlier_f64.d_flag;
     cudaMemset(d_cmpOffset, 0, sizeof(unsigned int)*cmpOffSize);
-    cudaMalloc((void**)&d_locOffset, sizeof(unsigned int)*cmpOffSize);
     cudaMemset(d_locOffset, 0, sizeof(unsigned int)*cmpOffSize);
-    cudaMalloc((void**)&d_flag, sizeof(int)*cmpOffSize);
     cudaMemset(d_flag, 0, sizeof(int)*cmpOffSize);
 
     // Decompression (implement for now, will update later with adaptive vectorization)
@@ -295,8 +297,4 @@ void cuSZp_decompress_3D_outlier_f64(double* d_decData, unsigned char* d_cmpByte
     dim3 gridSize(gsize);
     cuSZp_decompress_kernel_3D_outlier_vec4_f64<<<gridSize, blockSize, sizeof(unsigned int)*2, stream>>>(d_decData, d_cmpBytes, d_cmpOffset, d_locOffset, d_flag, blockNum, dims, errorBound);
 
-    // Free memory that is used.
-    cudaFree(d_cmpOffset);
-    cudaFree(d_locOffset);
-    cudaFree(d_flag);
 }
\ No newline at end of file
